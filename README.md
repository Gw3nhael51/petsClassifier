# petsClassifier

A simple and clean pipeline for preparing the Cats vs Dogs dataset.  
It includes image validation, dataset cleaning, and a reproducible train/test split.  
Ready to be extended with a TensorFlow model.

## Usage

```bash
python index.py
```

```bash
pip install -r requirements.txt```
